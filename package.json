{
  "name": "sleuthr",
  "version": "1.0.0",
  "scripts": {
    "setup": "npm run setup:client && npm run setup:server",
    "setup:client": "cd sleuthr.client && npm i",
    "setup:server": "cd sleuthr.server && npm i"
  }
}
